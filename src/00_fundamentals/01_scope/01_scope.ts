// block scope sorrounded by curly braces // not acessible outside
{
    let a = 10
}

// function scope // not accessible outside
function functionScope () {
    let b = 20
}

// global scope
let c = 30